/// Helper for initializing and handling local notifications (tap, foreground).
abstract class LocalNotificationHelper {
  LocalNotificationHelper._();

  // TODO: init, show, onTap callback
  static Future<void> init() async {}
}
