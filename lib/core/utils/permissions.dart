import 'package:get/get.dart';

/// Permission request helpers (camera, storage, notifications).
class PermissionsUtil extends GetxService {
  // TODO: Request and check permissions via permission_handler
  Future<PermissionsUtil> init() async => this;
}
