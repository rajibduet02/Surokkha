/// Keys for persistent storage (SharedPreferences / secure storage).
abstract class StorageKeys {
  StorageKeys._();

  static const String token = 'auth_token';
  static const String userId = 'user_id';
  static const String locale = 'locale';
}
